# Host profile selector (local to this machine).
# set-hypr-profile.sh rewrites this file to point at the active profile.
$RepoRoot = /home/zack/projects/scripts/jakoolithyprl
source = $RepoRoot/profiles/laptop.conf
